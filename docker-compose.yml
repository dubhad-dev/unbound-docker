version: '3.8'

services:
  unbound:
    image: dubhad/unbound:1.12.0
    restart: always
    volumes:
      - ./root.hints:/usr/local/etc/unbound/root.hints:ro
    environment:
      - THREADS
      - SLABS