version: "3.8"

services:
  unbound:
    build: unbound
    restart: always
    volumes:
      - ./root.hints:/usr/local/etc/unbound/root.hints:ro
    environment:
      - THREADS
      - SLABS
    ports:
        - "53:53/tcp"
        - "53:53/udp"
        - "443:443"
