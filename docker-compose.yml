version: "3.8"

services:
  unbound:
    build: unbound
    restart: always
    volumes:
      - ./root.hints:/usr/local/etc/unbound/root.hints:ro
      - ./certs/proxybel.xyz.key:/usr/local/etc/unbound/cert.key:ro
      - ./certs/proxybel.xyz.pem:/usr/local/etc/unbound/cert.pem:ro
    environment:
      - THREADS
      - SLABS
    ports:
        - "53:53/tcp"
        - "53:53/udp"
        - "443:443"
